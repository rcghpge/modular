load("//bazel:api.bzl", "modular_py_library")

package(default_visibility = ["//max:consumers"])

# Concrete cascade runtimes (in-process LocalRuntime; remote variants later).
modular_py_library(
    name = "runtimes",
    srcs = [
        "__init__.py",
        "local.py",
    ],
    deps = [
        "//max/python/max/experimental/cascade/core",
    ],
)
