load("//bazel:api.bzl", "mojo_test")

[
    mojo_test(
        name = src + ".test",
        srcs = [src],
        deps = [
            "//max:internal_utils",
            "//max:layout",
            "@mojo//:stdlib",
        ],
    )
    for src in glob(["**/*.mojo"])
]
