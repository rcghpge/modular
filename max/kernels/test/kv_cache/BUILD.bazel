load("//bazel:api.bzl", "mojo_test")

[
    mojo_test(
        name = src + ".test",
        srcs = [src],
        deps = [
            "//max:internal_utils",
            "//max:kv_cache",
            "//max:nn",
            "@mojo//:stdlib",
        ],
    )
    for src in glob(["**/*.mojo"])
]
