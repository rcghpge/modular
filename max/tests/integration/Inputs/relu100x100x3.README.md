# TensorFlow: `relu100x100x3`

Generated with the following code:

```python
import tensorflow as tf

model = tf.keras.Sequential(
    [
        tf.keras.layers.Input((100, 100, 3), dtype="float32", name="input"),
        tf.keras.layers.ReLU(name="output"),
    ]
)
archive = tf.keras.export.ExportArchive()
archive.track(model)
archive.add_endpoint(
    name="serving_default",
    fn=model.call,
    input_signature=[tf.TensorSpec(shape=(None, 100, 100, 3), dtype=tf.float32)],
)
archive.write_out("relu100x100x3")
```
