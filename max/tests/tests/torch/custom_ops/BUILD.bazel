load("//bazel:api.bzl", "mojo_library")

package(default_visibility = ["//visibility:public"])

extra_deps = [
    "@mojo//:tensor_internal",
    "@mojo//:compiler_internal",
    "@mojo//:stdlib",
]

mojo_library(
    name = "custom_ops",
    srcs = glob(["**/*.mojo"]),
    deps = extra_deps + ["@mojo//:MOGGPrimitives"],
)
