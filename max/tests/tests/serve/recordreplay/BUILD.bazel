load("//bazel:api.bzl", "modular_py_test", "requirement")

package(default_visibility = [
    "//:__pkg__",
    "//max/tests:__subpackages__",
])

modular_py_test(
    name = "test_recordreplay",
    size = "small",
    srcs = glob(["**/*.py"]),
    deps = [
        "//max/python/max/serve/recordreplay",
        requirement("fastapi"),
        requirement("httpx"),
        requirement("msgspec"),
        requirement("pydantic"),
        requirement("pytest-asyncio"),
    ],
)
