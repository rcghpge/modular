load("//bazel:api.bzl", "modular_py_test", "requirement")

modular_py_test(
    name = "mojo-importer",
    srcs = glob(["*.py"]),
    data = glob(["*.mojo"]),
    mojo_deps = [
        "@mojo//:stdlib",
    ],
    target_compatible_with = select({
        "//:asan_macos": ["@platforms//:incompatible"],
        "//:tsan": ["@platforms//:incompatible"],
        "//:ubsan": ["@platforms//:incompatible"],
        "//conditions:default": [],
    }),
    deps = [
        "@mojo//:python",
        requirement("numpy"),
    ],
)
